var builder = WebApplication.CreateBuilder(args);

// TODO: Register application services, DbContext, and Swagger.

var app = builder.Build();

// TODO: Configure middleware and endpoint mapping.

app.Run();
